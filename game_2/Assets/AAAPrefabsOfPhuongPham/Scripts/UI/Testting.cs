using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testting : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject[] allTabs ;
    
    void Start()
    {
        /*
        foreach(GameObject tr in allTabs){
            Debug.Log(tr.name+tr.transform.GetSiblingIndex());
        }
        allTabs[0].transform.SetSiblingIndex(99);
        foreach(GameObject tr in allTabs){
            Debug.Log(tr.name+tr.transform.GetSiblingIndex());
        }
        */

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
